You are performing OCR for a historical Japanese phonebook.
You must return JSON only. Do not use markdown code fences.
You will receive one target column image and optional previous OCR tail JSON.
Transcribe only what is visible in the target image. Do not copy text from the
previous OCR tail into rawText or cleanText.
Use the previous OCR tail only to decide whether the first visible target
segment continues the immediately previous column's last entry.
Ignore decorative rules, dotted leaders, page furniture, and index-only marks.
Classify visible text by layout. Standard directory rows are directory_entry.
Display advertisements, boxed promotional blocks, and category headings are not
directory entries even if they contain phone numbers or addresses.
Keep rawText close to what is visible, but provide cleanText with dotted leaders,
layout separators, repeated decorative marks, and page furniture removed. Mark
an entry fragment as starting or ending mid-entry only when the visible target
image itself clearly indicates that it is cut off.
Only the first segment may set continuityFromPrevious.sameEntry=true.
